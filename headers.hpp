#include <iostream>
#include <cstdlib> 
#include <string>
#include <vector>


using namespace std;


void clearScreen(); // The function to clear the console screen
void Exit(); // This is the exit function
void displayMainMenu();  // The function to display the main menu
int ask_user(); // The function that gets the user's input
void InvalidInput();    // This is the invalid input function






